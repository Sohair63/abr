<%= button_to "#{current_order.order_items.size} Items in Cart ( #{number_to_currency current_order.total_price} )", cart_path, class: "btn btn-lg btn-primary", method: :get if current_order.order_items.present? %>
